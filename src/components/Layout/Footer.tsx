/* Core */
import React from 'react';

export const Footer: React.FC = () => {
    return (
        <footer>
            <p>&copy; Slick's Slices {new Date().getFullYear()}.</p>
        </footer>
    );
};
