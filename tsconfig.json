{
    "extends": "tsconfig-dva",

    /* File inclusion */
    "exclude": ["node_modules", "public", ".cache"],
    "include": ["**/*.ts", "**/*.tsx"],

    "compilerOptions": {
        /* Module resolution */
        "baseUrl": ".",
        "paths": { "@/*": ["src/*"] }
    }
}
